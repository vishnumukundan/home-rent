import 'package:flutter/material.dart';

class ListCard__widget extends StatelessWidget {
  const ListCard__widget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
